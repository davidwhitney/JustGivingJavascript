var jg = jQuery.noConflict();

(function ($) {
    $.extend(true, {

        init: function (settings) {
            settings = $.extend({apiKey:'',rootDomain:'',apiVersion:''}, settings);
        }
});

})(jQuery);